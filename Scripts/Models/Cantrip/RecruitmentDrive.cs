using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//Recruitment drive draws two additional cards
public class RecruitmentDrive : MonoBehaviour, CantripController
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
