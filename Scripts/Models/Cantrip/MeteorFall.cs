using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//Meteor Fall deals massive damage in a targeted area
public class MeteorFall : MonoBehaviour, CantripController
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
